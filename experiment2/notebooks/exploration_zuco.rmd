# Exploring data from ZuCo 2.0

```{r}
libary(tidytable)
library(ggplot2)
library(rmatio)
library(eeguana)

# install if needed
# if (!requireNamespace("BiocManager", quietly = TRUE))
#   install.packages("BiocManager")
# BiocManager::install("rhdf5")
library(rhdf5)

setwd("experiment2")
```

## Read data

```{r}
# inspect structure
h5ls(file.path("data", "ZuCo", "bip_YAC_NR5_EEG.mat"))

# read a dataset
data <- h5read(file.path("data", "ZuCo", "bip_YAC_NR5_EEG.mat"), "EEG")

install.packages("hdf5r")
library(hdf5r)


file <- H5File$new(file.path("data", "ZuCo", "bip_YAC_NR5_EEG.mat"), mode = "r")
```